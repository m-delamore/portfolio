/* const bars = document.querySelectorAll(".bar");

setInterval(() => {
    bars.forEach((bar, index) => {
        let randomHeight = Math.floor(Math.random() * 120) + 30; // Varying heights
        bar.style.height = randomHeight + "px";
    });
}, 200); */